//
// Created by mrx on 2022/11/14.
//
#include <functional>
#include <algorithm>
#include <iostream>
#include <numeric>
#include <vector>
#include <cmath>
#include <queue>
#include <array>
#include <map>

using i64 = long long;

void sol() {
	int n, m, s;
	std::cin >> n >> m >> s;
	std::vector<i64> w(m + 1);
	for (int i = 0; i <= m; ++i) {
		std::cin >> w[i];
	}

	std::vector<int> a;
}

int main() {
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);
	std::cout.tie(nullptr);

	sol();
	return 0;
}